<%= form_with model: current_user.active_relationships.build, class: "follow_user" do |f| %>
  <%= hidden_field_tag :followed_id, @user.id %>
  <%= f.submit "いいね", class: "btn btn-primary" %>
<% end %>